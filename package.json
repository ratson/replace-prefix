{
  "name": "replace-prefix",
  "version": "0.0.1",
  "description": "Replace prefix of a string.",
  "scripts": {
    "lint": "eslint --ext js,md .",
    "test": "ava",
    "posttest": "npm run lint"
  },
  "dependencies": {
    "escape-string-regexp": "^1.0.5"
  },
  "devDependencies": {
    "ava": "^0.21.0",
    "eslint": "^4.3.0",
    "eslint-config-concise": "^0.10.0",
    "eslint-config-concise-ava": "^0.10.0"
  },
  "repository": "ratson/replace-prefix",
  "license": "MIT",
  "engines": {
    "node": ">=6"
  },
  "files": [
    "*.md",
    "index.js"
  ]
}
